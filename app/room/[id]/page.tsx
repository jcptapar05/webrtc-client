import Room from "@/components/Room";
import { RoomNoSSR } from "@/components/RoomNoSSR";
import React from "react";

const page = () => {
 return (
  <div>
   <RoomNoSSR></RoomNoSSR>
  </div>
 );
};

export default page;
